{
    "language": {
        "name": "Language",
        "description": "Choose the language of reminder messages.",
        "type": "string",
        "default": "en",
        "enum": [
            "en",
            "fr"
        ]
    },
    "reminder-amount": {
        "name": "Reminder Amount",
        "description": "How often the reminder should appear.",
        "type": "int",
        "default": 30,
        "min": 1,
        "max": 999
    },
    "reminder-unit": {
        "name": "Reminder Unit",
        "description": "Choose if the reminder uses minutes or hours.",
        "type": "string",
        "default": "minutes",
        "enum": [
            "minutes",
            "hours"
        ]
    }
}
